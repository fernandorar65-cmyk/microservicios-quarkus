package kahoot.clabs.quiz.infrastructure.persistence.postgres.enums;

/**
 * Persistence enum for quiz difficulty. Domain enum will be introduced later.
 */
public enum QuizDifficultyJpa {
    EASY,
    MODERATE,
    HARD
}
